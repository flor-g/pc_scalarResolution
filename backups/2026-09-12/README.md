# What is in this folder, and what is not

**These files are the FINISHED state of 2026-09-12, not the pre-change snapshot the folder name
implies.**

This folder was created at the start of T8 as the backup taken *before* the realizability work, and
held `main.ipynb` and `theta_u_learned_reach.md` as they stood after T7 (code done, prose not
started). At the end of the session a `cp` of the finished files into this folder overwrote both,
and added `appendix_E.ipynb`. The pre-T8 snapshot is gone; its contents are byte-identical to
`../2026-09-12-after-prose/`.

- **Last surviving snapshot before any of this work:** `../2026-09-11/`, taken before T0, i.e.
  before θ_u was made learned anywhere.
- **Finished state:** `../2026-09-12-after-prose/`, and these files, which are the same bytes.

What the lost snapshot would have shown is the notebooks after T0–T7 and before T8's realizability
block and T10's prose pass. Nothing in the working record depends on it: T8 and T10 record their own
diffs against it in `theta_u_learned_reach.md` (T8.3 recorded the output diff line counts, and T10
records every prose site changed).
