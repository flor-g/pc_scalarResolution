# 2026-09-22, before the Eq. (A6) dangling-reference fix

Both notebooks at 13:47, before `code cell 1` and its E1 mirror stopped citing the deleted Eq. (A6).

**Both files are byte-identical to `ab4840d`** ("Appendix B: the degenerate ray measured, and the
reassurance moved onto the reason"), so both are recoverable with `git show ab4840d:<file>`. Kept
for symmetry; the change they precede is a docstring correction that moved no printed output.
